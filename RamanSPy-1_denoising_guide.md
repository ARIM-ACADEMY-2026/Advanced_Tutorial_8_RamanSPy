# ノイズ除去（Denoising）4手法のまとめ ― SavGol / Whittaker / Kernel / Gaussian

RamanSPy の `ramanspy.preprocessing.denoise` が提供する4つの平滑化（ノイズ除去）手法について、**数理的な意味・特徴・引数設定の考え方・選び方・利用上の留意点**をまとめます。`RamanSPy-1.ipynb` の「3-4. ノイズ除去」節の補足資料です。

---

## 0. 前提：ノイズ除去とは何をしているのか

測定スペクトル $y_i$（$i$ は波数点のインデックス）は、真の信号 $s_i$ にランダムノイズ $\varepsilon_i$ が乗ったもの、と近似できます。

$$ y_i = s_i + \varepsilon_i $$

ノイズ除去（平滑化）は、**隣り合う点どうしは滑らかにつながっているはず**という仮定のもとで、高周波成分（＝細かく振動するノイズ）を弱め、低周波成分（＝なだらかな信号）を残す **ローパスフィルタ**です。4手法はいずれもこの「ローパス」を実現しますが、**どういう重みで近傍点を混ぜるか（＝どんな周波数応答を持つか）** が異なります。

> **最重要の共通原則**：平滑化は「ノイズのスケール」に合わせて弱くかけるもので、「ピークのスケール」に合わせてはいけません。フィルタ幅が**最も鋭いピークの半値幅（FWHM）に近づく／超えると、ピークがなまり（丸まり）、高さ・幅・面積・位置がゆがみます**。定量解析ではこの歪みが致命的になります。

**単位の換算**：フィルタ幅は「データ点数」で指定します。物理単位（cm⁻¹）に直すには波数刻み $\Delta\tilde\nu$ を掛けます。

$$ \text{幅}[\mathrm{cm^{-1}}] \approx \text{窓点数} \times \Delta\tilde\nu\,[\mathrm{cm^{-1}/点}] $$

OS-127 の CuPc（回折格子 300 gr/mm）は $\Delta\tilde\nu \approx 3.4\ \mathrm{cm^{-1}/点}$ なので、`window_length=7` は約 $24\ \mathrm{cm^{-1}}$ 幅に相当します。**回折格子が異なると刻みも変わる**（1200 gr/mm なら $\sim 0.9\ \mathrm{cm^{-1}/点}$）ため、同じ点数でも実効的な平滑化幅は変わる点に注意してください。

---

## 1. Savitzky–Golay フィルタ ― `denoise.SavGol(window_length, polyorder)`

### 数理的な意味
奇数長 $m=$ `window_length` の**移動窓**の中で、次数 $p=$ `polyorder` の多項式を最小二乗フィッティングし、その多項式を**窓の中心点で評価した値**を平滑化後の値とします。窓を1点ずつずらして全点で繰り返します。

重要なのは、この操作が結局**固定係数の畳み込み（線形FIRフィルタ）**に帰着することです。

$$ z_i = \sum_{j=-(m-1)/2}^{(m-1)/2} c_j\, y_{i+j} $$

係数 $c_j$（Savitzky–Golay 係数）は $m$ と $p$ だけで決まります。多項式当てはめなので、**窓内の $p$ 次までのモーメント（ピークの高さ・曲率など）を保存**するのが本質です。

### 特徴
- **ピーク形状の保存に最も優れる**。単純平均と違い、ピーク頂点を平らにつぶしにくい。分光分野の事実上の標準手法。
- 導関数の計算にも使える（`deriv` 引数。RamanSPy 既定は 0＝平滑化のみ）。
- 線形フィルタなので高速・決定的（乱数なし）。

### 引数設定の考え方
| 引数 | 意味 | 設定の目安 |
| --- | --- | --- |
| `window_length` | 窓の点数（**奇数**） | **最も鋭いピークの FWHM（点数換算）を超えない**範囲で。CuPc（$\Delta\tilde\nu\approx3.4$）なら 5〜9 点（$\sim17$〜$31\ \mathrm{cm^{-1}}$）。旧版の 15 点（$\sim51\ \mathrm{cm^{-1}}$）はピーク幅より広く過平滑だった。 |
| `polyorder` | 当てはめる多項式の次数（`< window_length`） | 2〜4。次数を上げるほど微細構造を保つが、上げすぎるとノイズも拾う。窓が狭いときは 2〜3。 |

**直感**：`window_length` を大きく・`polyorder` を小さくするほど強い平滑化。両者はセットで効くので、まず窓幅をピーク幅以下に決め、次に次数を調整します。

### 留意点
- `window_length` は**必ず奇数**、かつ `polyorder < window_length`。
- 窓端（スペクトル両端）はパディングで処理されるため端の数点は信頼性が落ちる。先にクロッピングしておくとよい。

---

## 2. Whittaker（Whittaker–Henderson）平滑化 ― `denoise.Whittaker(lam, d)`

### 数理的な意味
**窓を使わず、スペクトル全体を一括で**滑らかにします。「元データへの忠実さ」と「滑らかさ」を天秤にかけ、次の目的関数を最小化する $z$ を求めます。

$$ Q(z) = \underbrace{\sum_i (y_i - z_i)^2}_{\text{忠実項}} \;+\; \lambda \underbrace{\sum_i \left(\Delta^{d} z_i\right)^2}_{\text{粗さペナルティ}} $$

ここで $\Delta^{d}$ は $d$ 階差分（$d=2$ なら2階差分＝曲率に相当）。解は線形方程式

$$ z = \left(I + \lambda D_d^\top D_d\right)^{-1} y $$

で与えられます（$D_d$ は $d$ 階差分行列）。**バンド行列**なので大規模でも高速に解けます。

### 特徴
- **大域的な滑らかさを1つのパラメータ $\lambda$ で連続的に制御**できる。窓長を選ぶ必要がない。
- 窓境界のつなぎ目アーティファクトが出ない、なめらかな結果。
- ベースライン補正の asLS/arPLS などと同系統（同じペナルティ最小二乗の枠組み）で、理解を共有できる。

### 引数設定の考え方
| 引数 | 意味 | 設定の目安 |
| --- | --- | --- |
| `lam`（$\lambda$） | 粗さペナルティの重み。**大きいほど強い平滑化** | **対数スケールで桁を振って調整**（例 $10^1, 10^2, 10^3\ldots$）。ノイズ除去目的なら小さめ（$\sim10^1$〜$10^2$）。旧版の $\lambda=1000$ は鋭いピークにはやや過平滑。 |
| `d` | 差分の階数 | 通常 **2**（曲率を罰する）。1 は傾き、3 は変曲を罰する。 |

**直感**：$\lambda$ は「どれだけ滑らかさを優先するか」のツマミ。感度が高いので、必ず何桁か振って結果を目で確認します。

### 留意点
- $\lambda$ を上げすぎると鋭いピークが強く広がる（大域ペナルティなのでピーク近傍も一律に平滑化される）。
- 物理単位に直接対応する幅がない（点数刻みに依存）ため、格子・刻みが違うスペクトル間で $\lambda$ を使い回すときは実効的な平滑化度合いが変わる点に注意。

---

## 3. カーネル／ウィンドウ平滑化 ― `denoise.Kernel(kernel_type, kernel_size)`

### 数理的な意味
サイズ $k=$ `kernel_size` の**カーネル（重み関数）$w_j$ による畳み込み**です。

$$ z_i = \sum_{j} w_j\, y_{i+j}, \qquad \sum_j w_j = 1 $$

`kernel_type="flat"` は重みが一様（$w_j = 1/k$）で、いわゆる**単純移動平均（ボックスカー）**になります。

### 特徴
- **最も単純・高速**で直感的。
- ただし `flat`（移動平均）は**ピーク保存性が4手法で最も悪い**。0次多項式当てはめに相当し、ピーク頂点を平らにつぶしやすい。
- ボックスカーは周波数応答が sinc 型で**リップル（副次的な通過帯）**を持ち、特定周波数のノイズを取り残すことがある。

### 引数設定の考え方
| 引数 | 意味 | 設定の目安 |
| --- | --- | --- |
| `kernel_type` | カーネル形状 | `"flat"`（移動平均）が基本。 |
| `kernel_size` | カーネルの点数（**奇数**） | 3〜5 点程度の小さめに留める。大きくするとピークが急速になまる。 |

### 留意点
- ピーク形状を重視する定量解析には不向き。**「とにかく軽く均す」「ピークが元々ブロードで速度優先」**といった場面に限定して使う。
- サイズは奇数を推奨（中心対称にするため）。

---

## 4. ガウシアンフィルタ ― `denoise.Gaussian(sigma, order, ...)`

### 数理的な意味
ガウス関数をカーネルとする畳み込みです。

$$ g(x) = \frac{1}{\sqrt{2\pi}\,\sigma}\exp\!\left(-\frac{x^2}{2\sigma^2}\right), \qquad z = g * y $$

$\sigma$（標準偏差、点数単位）がカーネル幅を決めます。`order=0` が平滑化、`order>0` はガウス微分（導関数）に相当します。

### 特徴
- **時間領域・周波数領域の両方でなめらか**（ガウスのフーリエ変換もガウス）で、ボックスカーのようなリップルが出ない、**素直で予測しやすいローパス**。
- ピークは畳み込みにより広がるが、その広がり方が明快：ガウス形ピークなら幅が二乗和で加わる。

$$ \sigma_{\text{出力}}^2 = \sigma_{\text{ピーク}}^2 + \sigma_{\text{フィルタ}}^2, \qquad \mathrm{FWHM}_{\text{gauss}} = 2.355\,\sigma $$

### 引数設定の考え方
| 引数 | 意味 | 設定の目安 |
| --- | --- | --- |
| `sigma` | ガウスカーネルの標準偏差（点数） | **小さめ（1〜2 点程度）**。$\mathrm{FWHM}\approx2.355\sigma$ がピーク FWHM を大きく超えないように。CuPc なら `sigma=1.5`（$\mathrm{FWHM}\approx3.5$ 点 $\approx12\ \mathrm{cm^{-1}}$）が目安。 |
| `order` | 微分階数 | 平滑化は **0**。 |

### 留意点
- $\sigma$ を上げるほど確実にピークが広がる。定量的にピーク幅を議論する場合は、フィルタによる広がり（$\sigma_\text{フィルタ}$）を意識する。
- 端はパディング（`mode`）に依存。既定の挙動を把握しておく。

---

## 5. 4手法の比較と選び方

### 一覧比較

| 手法 | 数理の型 | ピーク保存性 | 主な引数 | 速度 | 向いている場面 |
| --- | --- | --- | --- | --- | --- |
| **SavGol** | 移動窓・多項式最小二乗（FIR） | ◎ 最良 | `window_length`, `polyorder` | 速 | 鋭いピークの形状・高さ・面積を保ちたい（分光の既定） |
| **Whittaker** | 大域ペナルティ最小二乗 | ○ | `lam`, `d` | 速（バンド行列） | 窓長を選ばず、$\lambda$ 一つで大域的な滑らかさを調整したい |
| **Gaussian** | ガウス畳み込み | ○（予測可能な広がり） | `sigma` | 速 | 素直でリップルのない平滑化。ノイズが強くピークがやや広いとき |
| **Kernel(flat)** | 移動平均（畳み込み） | △ 最も低い | `kernel_type`, `kernel_size` | 最速 | 最も簡便。ピーク保存より速度・単純さ優先の粗い平滑化 |

### 選び方のフローチャート（実務の目安）

1. **まず SavGol を既定に**する。ピーク形状の保存が最も良く、分光では第一選択。窓幅をピーク FWHM 以下に設定する。
2. **窓長の選択に悩む／大域的な滑らかさを一つのツマミで連続的に調整したい** → **Whittaker**（$\lambda$ を対数で振る）。
3. **リップルのない素直な平滑化がほしい／ピークが元々ブロードで、広がり方を明快に見積もりたい** → **Gaussian**。
4. **とにかく軽く速く均せればよい（定量解析はしない）** → **Kernel(flat)**。ただしピークはなまりやすい。

> 4手法は「どれか一つが常に正解」ではありません。**同じスペクトルに複数をかけて、ピークの高さ・幅・S/N を見比べて選ぶ**のが実践的です（`RamanSPy-1.ipynb` の窓幅比較図と同じ発想）。

---

## 6. 利用における共通の留意点

- **過平滑化を避ける**：フィルタ幅は「ノイズ周期」に合わせ、「ピーク幅」に近づけない。目安は**最も鋭いピークの FWHM（点数）より狭い**フィルタ幅。過平滑はピーク高さ・面積比を崩し、定量解析・ケモメトリクスの結果をゆがめる。
- **前処理の順序**：宇宙線スパイクは平滑化を大きく乱すため、**平滑化の前に宇宙線除去（despike）を必ず行う**。平滑化はベースライン補正の前に置くと補正が安定する（`RamanSPy-1` のワークフロー参照）。
- **平滑化はノイズ除去であって背景除去ではない**：緩やかな蛍光背景（低周波成分）はローパスでは取れない。それは**ベースライン補正**の役割。役割を混同しない。
- **点数と物理単位の対応**：フィルタ幅は点数指定。cm⁻¹ に直すには波数刻みを掛ける。**回折格子・励起波長が違うと刻みが変わる**ため、複数スペクトルを一括処理する際は「同じ点数窓＝同じ cm⁻¹ 幅」ではないことに注意（共通波数グリッドへリサンプリングすれば刻みは揃う。`RamanSPy-2` 参照）。
- **奇数制約**：SavGol の `window_length`、Kernel の `kernel_size` は奇数（中心対称）にする。SavGol は `polyorder < window_length`。
- **端の扱い**：窓型（SavGol/Kernel/Gaussian）は両端の数点がパディング依存で信頼性が下がる。関心領域より広めに測定し、先にクロッピングしておく。
- **決定的（再現性）**：4手法とも乱数を使わない決定的処理。同じ入力・同じ引数なら結果は常に同一（乱数シード不要）。
- **線形フィルタとしての限界**：4手法はいずれも（SavGol も含め）本質的に線形ローパス。**鋭いピークと高周波ノイズは周波数が重なる領域があり、完全な分離はできない**。ノイズを消しきろうとすると必ずピークも削れる、というトレードオフを前提に、控えめにかけるのが基本。

---

### 参考
- Savitzky, A.; Golay, M. J. E. "Smoothing and Differentiation of Data by Simplified Least Squares Procedures." *Anal. Chem.* **1964**, *36*(8), 1627–1639.
- Eilers, P. H. C. "A Perfect Smoother." *Anal. Chem.* **2003**, *75*(14), 3631–3636.（Whittaker 平滑化）
- RamanSPy 公式ドキュメント（Preprocessing）: https://ramanspy.readthedocs.io/en/latest/preprocessing.html
